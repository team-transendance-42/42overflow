for llm: system interface:
---------------------------
*  display only last 10 q -> done and pushed

*** rate limit msg: err msg if over 20msg per user, over 2msg per min-> TODO: got it at q 24 but had a few q without a reply: what we count: check 

*** connect with authenication: logged in user: jwt, cookie: check what technology we use

 *** if hold and release dictate : but nth said: whisper halicunates: fix!!
----------------------------
