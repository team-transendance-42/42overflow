RAG_ADMIN_TOKEN=your-secret-token

# Docker service hostnames — match docker-compose.yml service names
CHROMA_URL=http://chromadb:8000
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/transcendance_db

# Override to use a larger model on machines with more RAM (wipe chroma volume if you change this)
# EMBED_MODEL=nomic-ai/nomic-embed-text-v1.5
