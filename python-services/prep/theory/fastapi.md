Whisper models are downloaded and cached locally on first use—after that, you can use them offline.

FastAPI is a modern Python web framework for building APIs. It's fast, easy, and similar to Express (JS) or Gin (Go). It handles HTTP requests, routing, and data validation.