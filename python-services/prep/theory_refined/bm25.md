  BM25          → catches exact term matches, very fast, O(N)
  NumpyIndex    → catches semantic similarity, "ptr" ≈ "pointer" in embedding space
  RRF merge     → combines both ranked lists by position
============
  The embedding model (nomic-embed-text or BAAI/bge-small) was trained on billions of sentences. It knows:
  - ptr and pointer are semantically close → similar vectors → high cosine score
  - concurrency and multithreading overlap heavily in meaning → similar vectors

