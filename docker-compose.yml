services:
  postgres:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: transcendance_db
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@postgres:5432/transcendance_db"
    depends_on:
      - postgres
  
  # llm-server:
  #   build:
  #     context: ./llm-system-interface
  #     dockerfile: Dockerfile  # ← Create this
  #   ports:
  #     - "8081:8081"  # Only via docker, not exposed to internet
  #   environment:
  #     GEMINI_API_KEY: ${GEMINI_API_KEY}
  #   env_file:
  # - ./llm-system-interface/.env      # Load from this file
  #   depends_on:
  #     - postgres
  
  # nginx:
  #   image: nginx:latest
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf
  #   depends_on:
  #     - app
  #     - llm-server

volumes:
  postgres-data:
