1. check if you are in your branch
2. when you decide you are ready to merge into develop:
	build the project(docker compose up -d --build): try all functionality that it works
3. always first pull from develop: git pull origin develop
 push into your branch;
 go on github from your browser: create merge request;
 very important: check that you want to merge with develop, not main;
 ask someone to review:
	if successfull: they merge
	if not: comeback with what doesnt work

4. merge conflict: everyone is responisble to fix their conflict with develop
if assistance needed: you are welcome to ask for help

good luck :)